# mod_upload_ui()

UI module for upload

## Usage

``` r
mod_upload_ui(id, dev = FALSE)
```

## Arguments

- id:

  module id

- dev:

  show developer reactive-value outputs (default FALSE)

## Value

A `tagList` of the upload module's UI (file inputs and preview tables
for `base` and `compare`)
