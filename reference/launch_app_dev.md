# launch_app_dev

dev variant of
[`launch_app()`](https://mjfrigaard.github.io/dfdiffs/reference/launch_app.md)
with the modules' debug reactive-value outputs turned on (`dev = TRUE`)

## Usage

``` r
launch_app_dev()
```

## Examples

``` r
if (FALSE) { # \dontrun{
launch_app_dev()
} # }
```
