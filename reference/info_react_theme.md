# reactable theme (neutral info tables)

theme for small neutral info/reference tables (e.g. intersecting
columns, compare columns) shown in `mod_select`/`mod_compare`

## Usage

``` r
info_react_theme(dark = FALSE)
```

## Arguments

- dark:

  use dark-mode colors (default FALSE)
