% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare_values.R
\name{compare_values}
\alias{compare_values}
\title{Compare two vectors of values (base R)}
\usage{
compare_values(
  base_val,
  compare_val,
  tolerance = sqrt(.Machine$double.eps),
  scale = NULL,
  strict_factor = FALSE
)
}
\arguments{
\item{base_val}{vector of base/previous values}

\item{compare_val}{vector of compare/current values}

\item{tolerance}{numeric tolerance for numeric/date-time comparisons
(default \code{sqrt(.Machine$double.eps)}, matching \code{diffdf::diffdf()})}

\item{scale}{optional scale applied before the tolerance check (default
\code{NULL}, i.e. no scaling)}

\item{strict_factor}{if \code{TRUE}, a factor compared against a non-factor
is always flagged as different, regardless of label (default \code{FALSE},
which compares factor/character pairs by label)}
}
\value{
logical vector, \code{TRUE} where \code{base_val}/\code{compare_val} differ
}
\description{
Element-wise value comparison used by \code{diff_values_by_key()}
in place of \code{diffdf::diffdf()}. NA-safe (\code{NA} vs \code{NA} is never
"different"; \code{NA} vs a value always is). Numeric values (including a
mix of integer/double) are compared with tolerance, matching
\code{diffdf:::compare_vectors.numeric()}'s formula. \code{Date}/\code{POSIXct} values
are compared as epoch seconds with the same tolerance, so a \code{Date} and
a \code{POSIXct} representing the same instant compare as equal. Factors are
cast to character before comparing (label comparison), unless
\code{strict_factor = TRUE}, in which case a factor/non-factor class
mismatch is always flagged as different.
}
