% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare_values.R
\name{diff_values_by_key}
\alias{diff_values_by_key}
\title{Compare matched rows of two data frames by key (base R)}
\usage{
diff_values_by_key(
  base,
  compare,
  by = NULL,
  tolerance = sqrt(.Machine$double.eps),
  scale = NULL,
  strict_factor = FALSE
)
}
\arguments{
\item{base}{a data.frame or tibble}

\item{compare}{a data.frame or tibble}

\item{by}{character vector of shared key column(s), or \code{NULL} to match
rows by position (base row 1 vs. compare row 1, etc.)}

\item{tolerance, scale, strict_factor}{passed to \code{compare_values()}}
}
\value{
a list with \code{diffs} (long format: \verb{Variable name}, \code{by} key
column(s), \verb{Current Value}, \verb{Previous Value}), \code{diffs_byvar} (count of
differing values per variable), and \code{class_diffs} (class mismatches by
shared column)
}
\description{
Base-R replacement for \code{diffdf::diffdf()}. Matches \code{base}
and \code{compare} rows on \code{by}, then compares every other shared column
row-by-row with \code{compare_values()}. With \code{by = NULL}, rows are matched
by position instead (matching \code{diffdf::diffdf()}'s own behavior when
\code{keys} is unset).
}
